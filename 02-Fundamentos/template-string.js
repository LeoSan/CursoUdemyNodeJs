const nombre = 'Jose'; 
const real   = 'Leonard';

const normal = nombre + '  ' +real;

console.log(normal);

//Forma template string -> hacer una interpolación o union de caracteres y operaciones 

console.log(`\n Tu nombre real es ${nombre} y ${real} ${ 1 + 2 } \n`);

const html = `

<h1>Hola<h1> 
<p>Mundo<p> 

`;

console.log(html);